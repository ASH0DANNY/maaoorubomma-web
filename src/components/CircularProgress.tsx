import { Box } from '@mui/material'

const CircularProgress = () => {
    return (
        <Box sx={{ display: 'flex' }}>
            <CircularProgress />
        </Box>
    )
}

export default CircularProgress